// Layout untuk dashboard sudah ditangani oleh parent layout di app/admin/layout.tsx
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
